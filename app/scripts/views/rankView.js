define(['backbone'], function(Backbone) {
  var RankView = Backbone.View.extend({
    el: '.main',
    initialize: function() {
      console.info(212);
    },
    render: function() {},
    events: {}
  });

  return RankView;
});
